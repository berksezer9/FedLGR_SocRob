numpy==1.22.4
scikit-learn==1.2.1
matplotlib==3.5.3
grpcio==1.51.3
# Pinned to 1.12.0: the last flwr release confirmed (via source inspection,
# github.com/adap/flower, tag v1.12.0 vs v1.13.0) to still ship
# flwr.simulation.start_simulation -- the classic Ray-based simulation API
# main.py/main_fcl.py call. 1.13.0 removed it in favor of the new
# SuperLink/ServerApp/ClientApp architecture, which is a rewrite this repo
# does not use. See OFFICEDB_MODIFICATIONS.md.
flwr[simulation]==1.12.0
iterators==0.0.2
torch==2.0.1
torchaudio==2.0.2
torchmetrics==0.11.4
torchvision==0.15.2
tornado==6.3.2
tqdm==4.65.0
seaborn==0.12.2
albumentations==1.3.1

